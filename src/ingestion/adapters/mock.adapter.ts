import type { SourceAdapter } from '@/domain/sources/adapter';
export const mockAdapter: SourceAdapter = { key: 'mock.manual', async run() { return { rawPayload: { source: 'seed' }, normalized: [{ title: 'Lisbon metro strike (sample)', summary: 'Sample disruption for development', description: 'Sample-only event for local development and tests.', category: 'transport_strike', status: 'confirmed', severity: 'high', startsAt: new Date().toISOString(), sourceUrl: 'https://example.org/source', municipality: 'Lisboa', district: 'Lisboa', country: 'PT', verificationStatus: 'candidate' }] }; } };
